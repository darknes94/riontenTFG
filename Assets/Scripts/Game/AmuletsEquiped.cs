using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AmuletsEquiped : MonoBehaviour
{
    List<Slot> amuletEquiped = new List<Slot>();

    /*amuletEquiped.Add("Compsognathus");

foreach(string amlt in amuletEquiped)
{
    Console.WriteLine(amlt);
}

Console.WriteLine("\nCapacity: {0}", amuletEquiped.Capacity);
Console.WriteLine("Count: {0}", amuletEquiped.Count);

Console.WriteLine("\nContains(\"Deinonychus\"): {0}",
    amuletEquiped.Contains("Deinonychus"));

Console.WriteLine("\nInsert(2, \"Compsognathus\")");
amuletEquiped.Insert(2, "Compsognathus");
amuletEquiped.remove("Compsognathus")
amuletEquiped.clear()*/

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
